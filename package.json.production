{
  "name": "hitchbuddy",
  "version": "1.0.0",
  "type": "commonjs",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:react && echo 'React build complete'",
    "build:react": "cd client && npm install && npm run build && cp -r dist/* ../dist/public/",
    "start": "node deploy-server.cjs"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}