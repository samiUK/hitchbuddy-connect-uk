services:
  - type: web
    name: hitchbuddy-connect-uk
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node deploy-server.cjs
    envVars:
      - key: NODE_ENV
        value: development
      - key: FORCE_DEV_MODE
        value: 'true'
      - key: PORT
        value: '10000'
    healthCheckPath: /health
    autoDeploy: false